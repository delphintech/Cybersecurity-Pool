# SCORPION
The scorpion program receive image files as parameters and display the EXIF and metadata.

## Setup

`make` Create environment and executable

`make clean` stop environment and delete exec

## Usage
    ./scorpion FILE1 [FILE2 ...]